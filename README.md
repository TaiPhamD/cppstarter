# Basic CPP starter for MacOS

The starter project for C++ development on MacOS using CMake. This starter project requires:

- brew install cmake
- brew install llvm
- VSCode Extensions: 
  - CMake Tools
  - Clang-Format